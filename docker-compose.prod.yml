version: "3.8"

services:
  nextjs:
    container_name: nextjs-app
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
